#ifndef H_TABLE
#define H_TABLE

class Table {
   public:
      Table();   // This is the constructor declaration
      ~Table();  // This is the destructor: declaration
      void printTable();
   private:
};

#endif
